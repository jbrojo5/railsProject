<h1>All Tags</h1>

<ul id="tags">
  <% @tag.each do |tag| %>
    <li>
      <%= link_to tag.name %>
      <%= link_to " Delete", tag_path(tag), method: :delete, data: {confirm: "Are you sure you want to delete the #{tag.name} tag?"} %>
    </li>
  <% end %>
</ul>
